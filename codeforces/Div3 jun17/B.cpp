#include <bits/stdc++.h>
using namespace std;

int main(){

    int t;
    cin >> t;

    while (t--)
    {
        int n;
        cin >> n;
        string s;
        cin >> s;

        int x=0;
        int y = n-1;
        vector<int>a,b;
        int a1,b1 =0;
        a.push_back(s[0]);
        b.push_back(s[n-1]);

        for (int i = 0; i < n; i++)
        {
            if()
        }
        

   
    }

}